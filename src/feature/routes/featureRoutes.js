import express from "express";
import ensureAuthenticated from "../../middleware/authMiddleware";
import { attachedUser } from "../../middleware/atttachedUser.js"
const router = express.Router();

router.get()

export default router;


