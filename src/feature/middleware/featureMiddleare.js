import { logError, logInfo } from "../../utils/logger";

import { } from "../controller/featureController.js"

import attachedUser from "../../middleware/atttachedUser.js"
import { ensureAuthenticated } from "../../middleware/authMiddleware.js";

export const featureMiddleware = async (req, res) => {

}